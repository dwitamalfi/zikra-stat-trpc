import list, { List } from "postcss/lib/list"

export type ActivitySchema = {
  id: string
  user_id: string
  date: string
  page: string
  action: string
  created_at: string
  updated_at: string
  email: string
}
